#include <stdio.h>

/* Function delcared with int return data type must return an int. */
int main(int argc, char *argv[])
{
	int distance = 100;

	// this is also a comment
	printf("You are %d miles away.\n", distance);
	
	// int main must return an int, so just return 0;
	return 0;
}
